name: flash_list_view
description: A high-performance list view for Flutter.
version: 0.0.1
publish_to: none
homepage: https://github.com/definev/flash_list_view

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0